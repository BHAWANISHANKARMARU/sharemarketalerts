import ProductsExperience from "../components/platform/ProductsExperience";

export const metadata = {
  title: "Products — ShareMarketAlerts",
  description: "Explore the connected market intelligence, signal and alert platform.",
};

export default function ProductsPage() {
  return <ProductsExperience />;
}
